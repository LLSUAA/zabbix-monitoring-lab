# 按 **“现象 -> 分析 -> 解决 -> 反思”** 的结构展示部署中的问题

# 一、故障主题：**MySQL拒绝访问**
## 现象：
`ERROR 1045 (28000)`
## 排查思路：
1. 查MySQL错误日志  
2. 搜索发现是MySQL 8.0默认认证插件变更
3. 使用 `skip-grant-tables` 进入安全模式修改
## 根本原因与解决：
MySQL 8.0将`mysql_native_password`改为`caching_sha2_password`，旧客户端不兼容  
**解决**：修改root用户的认证插件

# 二、故障主题：**Zabbix前端404**
## 现象：
浏览器显示Nginx 404
## 排查思路：
1. 检查Nginx配置和访问日志  
2. 检查PHP-FPM服务状态和socket  
3. 检查文件权限和路径
## 根本原因与解决：
Nginx的 `fastcgi_pass` 参数与PHP-FPM实际监听的socket路径不匹配  
**解决**：统一两者配置，并重启服务

# 三、故障主题：**Agent显示不可用**
## 现象：
主机列表ZBX图标红色
## 排查思路：
1. 分层检查：服务进程 -> 端口监听 -> 网络防火墙 -> 配置参数 
2. 双向测试：Server端 `telnet`/`zabbix_get`  
3. 查看Agent端详细日志
## 根本原因与解决：
旧版`zabbix_agent`进程未关闭，占用10050端口，导致新版`zabbix_agent2`启动失败  
**解决**：`pkill zabbix_agent`，确保`zabbix_agent2`独占端口


# 四、故障主题：**Apache监控误报**
## 现象：
服务正常，Zabbix报“进程停止”。
## 排查思路：
1. 确认服务状态：`systemctl status apache2`
2. 检查Zabbix监控项配置，发现使用宏`{$APACHE.PROCESS_NAME}`
## 根本原因与解决：
Zabbix模板中的宏`{$APACHE.PROCESS_NAME}`在Ubuntu上默认为`httpd`，而实际进程名为`apache2`  
**解决**：在主机或模板级别将宏值修改为`apache2`

# 五、故障主题：**邮件报警失败**
## 现象：
触发器触发，但收不到邮件
## 排查思路：
1. 在Zabbix Web界面使用“测试”功能  
2. 在服务器命令行使用`sendemail`工具测试，发现SSL库缺失  
3. 查看Zabbix Server日志
## 根本原因与解决：
系统缺少Perl的SSL库，导致邮件发送函数失败。  
**解决**：安装 `libio-socket-ssl-perl` 和 `libnet-ssleay-perl` 库
